apiVersion: v1
kind: Pod
metadata:
  name: configmap-complete-pod
spec:
  containers:
    - name: app
      command: ["sh", "-c", "sleep 3600"]
      image: busybox
      envFrom:
        - configMapRef:
            name: api-configuration
